# Ticket-Check-Bro 自我进化协议（Self-Evolution Protocol）

本文件定义项目的"自我进化"机制：在每次代码提交后，自动评估健康度、记录进化轨迹、生成改进建议，并对退化行为设门禁拦截。

## 目标

让项目在无人值守的情况下持续产生**可观测的成长信号**，而非仅依赖人工复盘：

1. **健康度评估** — 量化当前工程质量（测试、构建、依赖、扩展性）。
2. **进化轨迹记录** — 每次提交追加一条时间线到 `docs/evolution-log.md`，形成成长档案。
3. **改进建议生成** — 基于静态规则自动产出下一步 TODO，降低"下一步该干什么"的决策成本。
4. **回归门禁** — 测试数下降或测试失败即拦截，防止静默退化。

## 触发点

- **CI（主路径）**：`.github/workflows/evolve.yml` 在 `push` 到 `main` 后自动运行 `node scripts/evolve.mjs`，并在 `build` 成功后传入 `BUILD_OK=1`。
- **本地（手动）**：`npm run evolve` 或 `node scripts/evolve.mjs`，用于提交前自检。

## 健康度评分模型（0–100）

| 维度 | 权重 | 评分规则 |
|------|------|----------|
| 测试 | 40 | 失败为 0 时满分；否则按通过率折算 |
| 构建 | 20 | `dist/index.html` 存在或 `BUILD_OK=1` 即满分 |
| 依赖健康 | 15 | ≤35 个满分；≤45 得 8；否则 3 |
| 扩展能力 | 15 | parser 插件 ≥8 满分；≥4 得 8；否则 3 |

## 记录格式

每次运行向 `docs/evolution-log.md` 追加一条：

```
## [version] ISO时间 — commitHash
- 健康度评分 / 门禁状态
- 测试通过率
- 结构指标（src 文件数 / parser 插件数 / 依赖数）
- 产物体积 / 构建状态
- 改进建议列表
```

历史快照（用于回归比较）持久化在 `docs/.evolution-state.json`，纳入 git 跟踪。

## 门禁规则（CI 失败即阻断）

| 条件 | 退出码 | 含义 |
|------|--------|------|
| 测试失败数 > 0 | 1 | 质量不达标 |
| 本次测试总数 < 上次记录总数 | 2 | 测试被误删 / 回归 |

健康度 < 60 不会阻断合并，但会在日志中标红提示，作为技术债信号。

## 改进建议规则（当前实现）

- 依赖数 > 45 → 提示审查未使用依赖
- 产物 > 5MB → 提示懒加载 / 拆分 chunk
- 测试数 < 20 → 提示补核心 parser 单测
- 未接入 ESLint/Prettier → 提示补齐护栏

## 扩展指南

新增"进化动作"只需在 `scripts/evolve.mjs` 中追加一个采集+评分+建议段落，保持零新依赖原则。
